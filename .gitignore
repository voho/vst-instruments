# macOS metadata
.DS_Store

# Editor / IDE state
.idea/
.vscode/
*~

# Build output (each instrument also ignores its own build dirs)
/build*/
/out/
/cmake-build-*/

# Scratch worktrees created by agent tooling: each is a full checkout of this
# repository, so committing one would nest the tree inside itself.
/.claude/worktrees/
