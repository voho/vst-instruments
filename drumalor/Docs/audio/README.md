# Drumalor demonstration audio

Seven rendered examples of what Drumalor produces: the full thirteen-voice
vocabulary one hit at a time, two programmed grooves, the snare played from a
ghost note to a full accent, a tom and cymbal phrase, the Humanise control
heard on a repeated bar, and the kit bus pushed on a loop.

Every file here is rendered by [`Tools/RenderDemos.cpp`](../../Tools/RenderDemos.cpp)
from the shipping JUCE-free signal path — the same `DrumEngine` the VST3, Audio
Unit and Standalone run — so a demonstration cannot drift away from what the
plug-in actually sounds like. No samples, impulse responses, reverbs or external
processing are involved anywhere in this directory: every hit is synthesized on
the spot, and every voice is triggered through the same General MIDI note map a
pad controller uses. The grooves are programmed on a sixteenth grid at a stated
tempo, exactly as a sequencer would play the instrument.

## How to listen to them

- **01** is the instrument itself: all thirteen voices of the factory kit in
  note order — Kick, Snare, Clap, Closed and Open Hats, Ride, Crash, the three
  toms, Shaker and the two percussion voices — each at the same velocity, with
  each voice given the ring its factory decay asks for.
- **02** is a plain rock beat at 96 BPM. Listen for the open hat at the end of
  every second bar: the next bar's closed hat cuts it off, because the two hats
  share choke group A out of the box, like a pedal.
- **03** is a busier break at 104 BPM. The ghost notes are ordinary snare hits
  at low velocity — velocity drives the struck timbre and the wire content as
  well as the level, so the ghosts darken rather than merely getting quieter.
- **04** is that velocity axis in isolation: one snare, eight hits from a ghost
  stroke to a full accent, with nothing else changing between them.
- **05** takes the backbeat away: ride time with crashes on the turns, tom
  answers, and a descending sixteenth fill across all three toms into a final
  crash.
- **06** plays the same bar six times: two bars machine-tight (Humanise at
  zero), two at the factory depth, two doubled. The grid never moves — what
  changes is the per-hit pitch, decay, timbre and drive tolerance, modelled as
  the slow component drift of an analogue voice.
- **07** plays one loop three ways: dry, through Bus Drive, then glued by Bus
  Compression. The compressor's fast attack and slow release pump the hats
  after each kick, which is easiest to hear in the final third.

## Levels

Each take is rendered with generous headroom and then normalised to −3 dBFS, so
the files can be auditioned one after another without reaching for the volume.
The renderer refuses to write a take that reached full scale before
normalisation, because such a file would have been pushed into the hard clamp
behind the engine's master saturator and would be documenting that clamp rather
than the kit.

The **rendered peak** column below is the level the engine produced before that
normalisation, relative to the plug-in's own output stage at the reduced gain
the renderer uses. It is the honest measure of how loud each take actually is:
the grooves with a full kit playing are genuinely far louder than the ghost
strokes at the start of the velocity take.

<!-- peaks-table-begin: regenerated by DrumalorRenderDemos; edits between the markers are overwritten -->
| File | What it is | Length | Rendered peak | Normalisation |
| --- | --- | ---: | ---: | ---: |
| `01-kit-vocabulary.wav` | All thirteen voices of the factory kit, one hit at a time | 12.6 s | −12.0 dBFS | +9.0 dB |
| `02-rock-groove.wav` | A plain rock beat at 96 BPM with the open hat choked by the closed | 11.7 s | −9.4 dBFS | +6.4 dB |
| `03-breakbeat-ghost-notes.wav` | A busier break at 104 BPM with ghost-note snare | 10.8 s | −12.0 dBFS | +9.0 dB |
| `04-snare-velocity.wav` | One snare from a ghost note to a full accent | 6.1 s | −14.4 dBFS | +11.4 dB |
| `05-toms-and-cymbals.wav` | Ride time, crashes and a descending tom fill | 13.1 s | −8.2 dBFS | +5.2 dB |
| `06-humanise.wav` | The same bar machine-tight, at the factory Humanise, then doubled | 13.4 s | −11.6 dBFS | +8.6 dB |
| `07-kit-bus.wav` | One loop dry, through Bus Drive, then glued by Bus Compression | 13.6 s | −10.7 dBFS | +7.7 dB |
<!-- peaks-table-end -->

## Regenerating them

```bash
cd drumalor
cmake -S . -B build-dsp -DCMAKE_BUILD_TYPE=Release \
  -DDRUMALOR_BUILD_PLUGIN=OFF -DBUILD_TESTING=OFF
cmake --build build-dsp --parallel --target DrumalorRenderDemos
./build-dsp/DrumalorRenderDemos Docs/audio
```

The render is deterministic, so running it on an unchanged engine rewrites the
same bytes and commits nothing: every per-hit humanisation value is hash-derived
from per-instrument trigger counters that reset with the engine, never from a
clock. Automation can therefore re-render this directory and commit the result
only when the audio has genuinely changed, which keeps it in lockstep with the
code. The table above is regenerated in place by the same command, so the
documented levels cannot drift away from the committed files either.

A take that is renamed or removed from the renderer's table is deleted from this
directory on the next full render, so a stale WAV cannot survive here after the
demo that produced it has gone.
